---
- name: provision runner startup script on our docker-swarm servers
  hosts: swarm-master
  gather_facts: no
  force_handlers: no
  become: no

  tasks:
    - copy:
        src: service-scripts/
        dest: /usr/local/bin/
        mode: 0755
      become: yes
